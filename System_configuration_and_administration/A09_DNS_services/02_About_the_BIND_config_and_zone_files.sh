#Working on Rocky01

#View the BIND configuration file with line numbers
sudo less -N /etc/named.conf

#List the contents of the BIND zone files directory
sudo ls -l /var/named

#View the default localhost zone file with line numbers
sudo less -N /var/named/named.localhost